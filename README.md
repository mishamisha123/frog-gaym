# Froggy Leap v114.3 — TRUE AUTHORITATIVE RESET

GITHUB / WEBSITE ONLY. Deploy matching Firebase v114.3 FIRST.

Reset flow: Firebase authoritative reset -> fresh Firebase read -> verify 1,000 F / Player Lv 1 / Job Lv 1 -> ONLY THEN clear local browser save -> fresh Cloud Save -> final Firebase verification. If Firebase still has the old wallet/levels, local progress is NOT erased and the HUD cannot fake a reset.

The top balance, Player Level and XP use the authenticated server snapshot. Upload extracted files to the GitHub Pages repository root, then use refresh.html.
